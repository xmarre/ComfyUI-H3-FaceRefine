ultralytics
scipy
# insightface declares CPU-only `onnxruntime`; install.py repairs it on install/update
# and prestartup_script.py reasserts the GPU provider after later dependency changes.
insightface
